# 検証記録と未確認事項

作成日：2026-09-17  
アプリ：0.1.0  
作成環境：Python 3.13.5 / Plotly 6.5.2 / pandas 2.2.3

## 実際に確認した範囲

`python -m unittest discover -s tests -v` により、全65件のうち**56件が実行されて合格、9件がスキップ**となりました。失敗0件です。実際のログは `TEST_LOG.txt` に同梱しています。

| 区分 | 実行数 | 結果 |
|---|---:|---|
| 光学関数 | 29 | 合格 |
| 主計算・出力 | 23 | 合格 |
| Plotlyグラフ生成 | 4 | 合格 |
| Streamlit UI | 9 | **未実行・スキップ** |

光学関数のテストには、1,000例の固定シードによるS/C/Axis往復変換、500例の独立した屈折力行列による加算検算、500例の頂点間距離往復換算、100例×1,800候補の密な軸探索との比較、200例の回転変動範囲の検算を含みます。これは**架空の数値ケース**であり臨床症例数ではありません。

SCL回転による残余乱視の既知の関係、CAASの時計回り／反時計回りの例、右眼／左眼で符号を反転しないこと、ゼロ残余時に二重補正しないこと、円柱度数不一致、0°/180°境界、同率候補、別の次回転量、プラス円柱入力、C=0の処理、入力不正値、JSON/CSV出力も確認しています。

グラフ生成のテストはPlotlyオブジェクトとシリアライズを検証するもので、ブラウザの表示やStreamlit画面の動作確認ではありません。

## UIを未実行とした理由

この作成環境にはStreamlitがなく、依存パッケージ導入時に外部パッケージサーバーへの接続が失敗しました。よってUIテストは明示的にスキップしています。**「起動確認済み」「UI全テスト合格」とはしていません。**

`.github/workflows/tests.yml` は、依存パッケージを導入した上でPython 3.12/3.13の計算・グラフ・UIテストを実行する設定です。ただし配布時点でユーザーのGitHub上で実行したわけではありません。

## 初回公開後に確認すること

1. GitHub Actionsが完了し、特に `TestStreamlitUI` の9件がスキップではなく合格していること。
2. PCと使用予定の端末で、全入力欄・計算ボタン・グラフ・結果保存が動作すること。
3. 時計回り10°のデモで10°、反時計回り10°のデモで170°が選ばれること。
4. 入力を変えると古い結果が消え、再計算まで表示されないこと。
5. 次S/Cや候補軸を変えると、その条件が結果とJSONに反映されること。
6. 実測の回転符号を独立した検者と照合し、装用後屈折値のVD設定が正しいこと。

## 未確認事項

患者を対象とした臨床的妥当性、装用後の視力・満足度・実際の残余乱視、度数変更後の回転の再現性、製品別の規格・在庫、クラウド基盤全体の安全管理、各施設での運用・第三者提供に必要な手続は未確認です。

臨床導入を判断する場合は、この数学的検算と独立した臨床検証を区別してください。
