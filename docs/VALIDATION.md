# v0.2.0 検証記録

確認日：2026年9月17日

## 実行した検証

作成環境：Python 3.13.5 / pandas 2.2.3 / Plotly 6.5.2。

`python -m unittest discover -s tests -v` を実行し、**102件中87件合格・15件スキップ・失敗0件**でした。実行ログは `TEST_LOG.txt` です。スキップは合格ではありません。

| 区分 | 実行数 | 結果 |
|---|---:|---|
| 既存の光学関数 | 29 | 合格 |
| 既存の既知回転エンジン・出力 | 23 | 合格 |
| Plotlyグラフ生成 | 4 | 合格 |
| 新しい入力仕様・検証・逆計算・状態管理 | 31 | 合格 |
| 新画面のStreamlit AppTest | 15 | 未実行・スキップ |

新しいテストには、指定刻みと端点、0.01 Dの浮動小数点誤差を避けた選択肢生成、全角数字・符号の正規化、刻み違反の検出、0°/180°の等価性、1000個の架空の回転ケース、異なる頂点間距離、プラス円柱、ゼロ残余時の二重補正回避、推定不能時の計算保留、古い回転入力を使わないこと、推定値としての出力表示を含みます。

架空のデモ1：候補表示軸10°、推定回転約+9.90695°、残余|C|約0.00435 D。

架空のデモ2：候補表示軸170°、推定回転約−9.90695°、残余|C|約0.00435 D。

04をS/C 0.01 D、Ax 1°へ丸めた表示値から計算しているため、デモの推定回転は厳密な±10°ではありません。画面に見えない未丸め入力値は使っていません。

## 未実行・未確認

作成環境にStreamlitはなく、パッケージ導入は外部ホストへの接続ができず成功しませんでした。したがって**実際のStreamlit画面・ブラウザ操作は未確認**です。

配布用requirementsはユーザーが起動できた設定（Streamlit 1.55.0 / Plotly 6.5.2 / pandas 2.3.3）を維持しています。ただしこの作成環境で当該組み合わせを再現したわけではありません。Python 3.14での実行もここでは未確認です。

GitHub ActionsはPython 3.12/3.13/3.14で依存を導入してテストする設定です。ユーザーのGitHub上での実行は本配布作業では行っていません。

実症例での回転推定の精度、軸交換後の予測残余屈折、視力、快適性、フィッティングの臨床検証は行っていません。

## 更新後の画面チェック

1. 上部にv0.2.0が表示され、03の回転入力がない。
2. 01/02/04のS/C/Axの刻みが指定どおりで、リスト選択と直接入力ができる。
3. 入力値を変えると古い結果が消え、再計算が必要になる。
4. デモ1で10°、デモ2で170°が候補となる。
5. 回転推定不能の入力に対して、0°を代入せず計算を保留する。
6. 推定であることと仮定が画面・JSON・TXTに表示される。
