# トーリックSCL 軸選択アプリ v0.3.0｜更新手順

更新日：2026年9月17日

## 起動中のアプリを更新する

今回は **app.py と engine.py の2ファイルを一緒に差し替えてください。**

1. 更新用ZIPを展開します。
2. GitHubの `trcscl` で、既存の `app.py` と `engine.py` が置かれている階層を開きます。
3. `Add file` → `Upload files` から、この版の `app.py` と `engine.py` をアップロードします。
4. 同名ファイルを更新する差分であることを確認して `Commit changes` を押します。
5. Streamlitの画面を開き直し、上部に **v0.3.0** と表示されていることを確認します。

**ZIPそのものをGitHubに置くだけでは更新されません。** 新しいフォルダを作るのではなく、既存ファイルと同じ場所に置きます。アプリの削除・再作成は不要です。

`requirements.txt`、`optics.py`、`visuals.py` は変更していません。現在動いている依存設定をそのまま使用します。

```text
streamlit==1.55.0
plotly==6.5.2
pandas==2.3.3
```

`engine.py` の変更は、詳細設定の「モデル不一致の確認閾値」で **0 Dも有効な選択肢**にするための範囲検証2行のみです。光学計算式・回転推定方式は前版から変更していません。閾値0 Dでは微小な数値誤差でも注意が出る場合があります。通常の初期値は従来どおり0.50 Dです。

## ご依頼の変更内容

| 欄 | S・Cのリスト | Axのリスト |
|---|---|---|
| 01 装用前の矯正値 | 0.25 D刻み | 5°刻み |
| 02 装用中SCLの表示度数 | 0.25 D刻み | 10°刻み |
| 03 装用後の屈折値（旧04） | 0.25 D刻み | 5°刻み |

### 数値リストは0始まり

S・Cは `0.00 → -0.25 → -0.50 → …` の順です。プラス値が入力可能な欄では、その後に `0.25 → 0.50 → …` が続きます。値を入力して検索・直接確定することもできます。

Axは01・03が `0 → 5 → 10 → … → 180`、02が `0 → 10 → 20 → … → 180` です。候補軸、頂点間距離、詳細設定の数値リストも0から始まります。

**リストの最初が0であることと、0を自動入力することは別です。** S・C・Axの初期入力欄は従来どおり空欄で、利用者が選択・入力します。0°と180°は同じ軸として計算し、結果では従来どおり180°表記になる場合があります。

### 直接入力はリストの刻みに制限されません

例えば `S -3.13`、`C -0.43`、`Ax 37`、`Ax 37.5` は、どの屈折欄でも直接入力できます。リスト外の数値を入力し、**Enterで確定**してください。入力値を0.25 Dや5°の格子へ丸める処理はありません。

任意入力でも数値としての検証は残します。

- S：−30〜+25 D。
- C：01・03は−15〜+15 D。02と次レンズはマイナス円柱（−15〜0 D）。現在のSCLはC≠0が対象です。
- Ax：0〜180°（小数可）。01・03のC=0では空欄可。
- 頂点間距離：0〜25 mm。回転変動シナリオ：0〜30°。不一致確認閾値：0〜5 D。いずれも範囲内の小数を直接入力できます。

全角数字・全角符号にも対応します。計算には入力精度の値を使用しますが、結果の表示桁数は従来どおりです。画面表示の丸めと計算前の入力値の丸めは区別しています。

### 番号・デモの整理

旧04「装用後の屈折値」は **03**、旧05「次に試せる軸の候補」は **04** に繰り上げました。説明、警告、TXT出力などの参照番号も変更しています。

デモボタン・デモ表示・デモ読込処理を削除しました。「入力をクリア」は残しています。前版のデモ状態や古い計算結果を持ち越さないよう、更新後の初回表示では入力状態をリセットします。内部テスト用の架空値はユーザー画面には表示しません。

## 確認済みの範囲

入力・計算・グラフの **97件が合格**、構文検査も合格しました。実際のStreamlit画面テスト15件は、作成環境でStreamlitを導入できなかったため未実行です。実画面での操作と公開先への反映は本作業では確認していません。

更新後はリスト先頭、任意入力、番号、デモ削除、計算、入力変更後の再計算を実画面で確認してください。

自動テストをGitHub Actionsで運用している場合は、旧画面用のテストではなく、完全版ZIPの `tests/` も更新してください。

## 計算・運用上の注意

この版はUI中心の変更です。回転を0°と仮定せず、01−03の屈折値から推定する従来の臨床未検証モデルを維持しています。推定値・仮定に依存し、結果だけで処方を確定するものではありません。

Streamlit上では入力はサーバー側で処理されます。個人情報を入力せず、施設の情報管理方針に沿って運用してください。

## ローカル実行とテスト

完全版のフォルダ内で実行します。

```bash
python -m pip install -r requirements.txt
python -m streamlit run app.py
python -m unittest discover -s tests -v
```

選択と直接入力の仕様参考：Streamlit公式 `st.selectbox`（accept_new_options）
https://docs.streamlit.io/develop/api-reference/widgets/st.selectbox
