# トーリックSCL 軸選択アプリ v0.2.0｜更新手順

更新日：2026年9月17日

## すでに起動できているアプリを更新する

**今回の画面・計算変更は `app.py` だけの差し替えで反映できます。**

1. GitHubの `trcscl` リポジトリで、`app.py` がある階層を開きます。
2. `Add file` → `Upload files` を選び、この版の `app.py` をアップロードします。
3. 同名の `app.py` を更新する差分になっていることを確認し、`main` に `Commit changes` します。
4. Streamlitの画面を開き直し、上部の版が **v0.2.0** であることを確認します。

現在動いている `engine.py`、`optics.py`、`visuals.py` は変更不要です。前回変更して起動できた `requirements.txt` も変更不要です。今回のZIPには同じ依存設定を収録しています。

```text
streamlit==1.55.0
plotly==6.5.2
pandas==2.3.3
```

**アプリの削除・再作成は不要です。ZIPそのものをGitHubへ置くだけでは更新されません。**

説明書や自動テストもそろえる場合は、ZIPを展開して中身を同じ階層に配置します。既存の `tests/test_ui.py` は旧画面用のため、テストを運用している場合は今回の版へ更新してください。新規の `tests/test_app_model.py` も追加します。

## 変更した入力仕様

03の「装用中SCLの回転」入力は削除しました。01、02、04、05の番号は以前の対応を保っています。

| 入力欄 | S・C | Ax |
|---|---|---|
| 01 装用前の矯正値 | 0.25 D刻み | 5°刻み |
| 02 装用中SCLの表示度数 | 0.25 D刻み | 10°刻み |
| 04 装用後の屈折値 | 0.01 D刻み | 1°刻み |

各数値欄は、**リストから選択することも、数値を直接入力してEnterで確定することもできます。**

入力候補の検索もできます。例えば04のC欄に `-0.43` と入力して確定します。全角数字や「−」のマイナス記号にも対応しています。指定刻み以外の値は自動丸めせず、計算時にエラーとして案内します。

S・Cの表示は小数点以下2桁です。Axの選択肢は指定間隔〜180°です。直接入力した0°は180°として扱います。01・04のC=0ではAxは不要です。02のSCLはマイナス円柱かつC≠0のトーリックSCLが対象です。

頂点間距離、詳細設定の数値、使用可能な候補軸も選択・直接入力に対応します。回転変動シナリオは詳細設定に残していますが、これは実測回転量の入力ではありません。

## 回転入力をなくしたことによる計算上の変更【重要】

回転入力欄を消して「回転0°」とみなす変更ではありません。

今回は、角膜面に換算した**01の矯正値−04の装用後残余屈折**からSCLの補正効果を推定し、その乱視ベクトルの方向から現在の眼上軸を推定します。02の表示軸との差を、次のレンズでも再現する回転として仮定します。

この方式では01は計算に参加します。以前のような独立した整合性確認用ではありません。

**これは臨床未検証の逆計算モデルです。実測回転の代替としての精度を保証しません。** 屈折状態が測定間で変わらず、SCLの効果を同一角膜面の薄レンズとして扱えることが前提です。推定値・仮定であることを画面、JSON、TXTで明記しました。

01−04の乱視成分がほぼゼロで眼上軸を推定できない場合は、計算を保留します。02の度数と推定レンズ効果が一致しない場合も注意を表示します。

## 更新後の確認

「デモ1を入力」→確認チェック→計算で候補表示軸10°、「デモ2を入力」で170°になることを確認してください。04の入力値は表示どおり0.01 D・1°に丸めており、内部だけで高精度値を使うことはありません。

その後、01のAxは5°刻み、02は10°刻み、04は1°刻みで選択できること、S・Cを選択・直接入力できること、変更後に古い結果が非表示になることを確認してください。

配布時の確認は計算・入力検証・グラフ生成までです。新画面の実際のブラウザ操作は作成環境では未確認です。詳細は `docs/VALIDATION.md` にあります。

## 新規配置・ローカル実行

新規配置は `app.py`、`engine.py`、`optics.py`、`visuals.py`、`requirements.txt` を同じ階層に置いてください。ローカルでは依存導入後に次を実行します。

```bash
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

テスト：

```bash
python -m unittest discover -s tests -v
```

## 公式資料

- Streamlit：GitHubの変更反映 https://docs.streamlit.io/deploy/streamlit-community-cloud/manage-your-app/edit-your-app
- Streamlit：選択と直接入力 https://docs.streamlit.io/develop/api-reference/widgets/st.selectbox
- GitHub：ファイルの管理 https://docs.github.com/en/repositories/working-with-files/managing-files

実データはStreamlitサーバー側で処理されます。患者名・IDは入力せず、施設の情報管理方針に沿って運用してください。
